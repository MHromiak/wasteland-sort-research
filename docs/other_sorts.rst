-----------
other_sorts
-----------

The module "other_sorts" holds a host of widely-used sorting algorithms. The module is usedd solely for data gathering and 
sorting result comparison. The actual comparison is done in stats/sort_compare.py

