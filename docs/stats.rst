-----
stats
-----

.. py:function:: generate_data(info: List[tuple])

    Generates randomly sampled integer sets based on the parameters of info

    :param info: List of tuples. tup[0] is the range of values, tup[1] is the number of sampled values from that range

    :returns: List of lists, each list being a sample